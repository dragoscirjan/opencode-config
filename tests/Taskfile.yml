version: "3"

includes:
  athena:
    taskfile: ./athena/Taskfile.yml
    dir: ./athena
  # clio:
  #   taskfile: ./clio/Taskfile.yml
  #   dir: ./clio
  hermes:
    taskfile: ./hermes/Taskfile.yml
    dir: ./hermes
  # hephaestus:
  #   taskfile: ./hephaestus/Taskfile.yml
  #   dir: ./hephaestus

tasks:
  test:
    desc: "Run all agents' tests"
    cmds:
      - task: hermes:test
      - task: athen:test
      # - task: hephaestus:test
      - task: clio:test
